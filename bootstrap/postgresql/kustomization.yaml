apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - job.yaml

configMapGenerator:
  - name: postgres-bootstrap-script
    files:
      - bootstrap.sh 

generatorOptions:
  disableNameSuffixHash: true
